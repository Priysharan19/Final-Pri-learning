# Pri local model assets

`PriInkFoundation.mlpackage` is generated by `tools/ink-foundation/export_coreml.py` after a checkpoint passes the real-writer release gates.

The app must remain functional when the asset is absent: development builds and pre-foundation releases fall back to the existing local Pri recogniser.
