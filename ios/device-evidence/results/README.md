# Physical iPad result files

Place one anonymised JSON result per physical iPad test run in this directory.

Do not commit student names, email addresses, Apple IDs, device serial numbers or other identifying information. Use a stable anonymised `testerId` and the schema documented in `../README.md`.

This directory being empty of JSON files means physical-device evidence is **NOT MEASURED**; that is an acceptable development state but not a passing strict release gate.
