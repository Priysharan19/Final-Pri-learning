// Caveat — the handwriting face (the shipped reel's hand). Loaded via
// @remotion/google-fonts so the renderer waits for it. Computer Modern comes
// from the katex package css (see Tex.tsx) — no network involved.
import { loadFont } from '@remotion/google-fonts/Caveat';

loadFont();
